Try to be a efficient tool that takes you to directories.

# Requirements
g++ with c++11 features enabled.

Currently makefile is not yet added, using the following cmds to create gt
```
g++ main.cpp -std=c++11 -o gt
```
